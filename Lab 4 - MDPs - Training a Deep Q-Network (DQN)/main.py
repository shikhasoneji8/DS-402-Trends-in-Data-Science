from testsMdpLab import *
#from testsProbPolicyLab import *
#from testsRlLab import *
from testsDqnLab import *
#from testsCriticalLab import *
#from testsPolicyIterLab import *

if __name__ == '__main__':
    '''
    This File is arranged into blocks representing the tests we have created for each of these labs.
    Note that as of Lab 1, we will not have provided files for Lab 2.
    This means you may get errors from the interpreter if you try to move ahead and use later tests.

    Many (but not all) tests take an argument which is the seed for the random number generator.
    Seeding in this way allows each of us to see the same output, which has some advantages.
    However, please feel free to change these seeds if you want to see different outputs from the 
    SAME configuration but with DIFFERENT random number sequences.
    '''


    # MDP lab tests
    # mdpLabTest1()
    #mdpLabTest2()
    #mdpLabTest3(seedValue=168)
    #mdpLabTest4(seedValue=465)
    #mdpLabTest5(seedValue=182152)
    #mdpLabTest6(seedValue=987537414)
    #mdpLabTest7(seedValue=24765129)

    '''
    # Prob Policy lab tests
    probPolicyLabTest1(seedValue=42)
    probPolicyLabTest2(seedValue=781234)
    probPolicyLabTest3(seedValue=773521)
    probPolicyLabTest4(seedValue=1691283)
    probPolicyLabTest5(seedValue=986124)
    probPolicyLabTest6(seedValue=5659173)
    probPolicyLabTest7(seedValue=19880123)
    '''

    '''
    # Reinforcement Learning lab tests
    RlLabTest1(seedValue=12987123)
    RlLabTest2(seedValue=71)
    RlLabTest3(seedValue=25871823)
    RlLabTest4(seedValue=8123192847)
    RlLabTest5(seedValue=1023123)
    RlLabTest6(seedValue=1203981923)
    RlLabTest7(seedValue=12123)
    '''

    
    # DQN Lab tests
    # DqnLabTest1(seedValue=10)
    # DqnLabTest2(seedValue=10)
    # DqnLabTest3(seedValue=10)
    # DqnLabTest4(seedValue=10)
    # DqnLabTest5(seedValue=10)
    # DqnLabTest6(seedValue=10)
    # DqnLabTest7(seedValue=10)
    # DqnLabTest8(seedValue=10)
    # DqnLabTest9(seedValue=10)
    DqnLabTest10(seedValue=10)
    

    '''
    # Critical States lab tests
    CriticalLabTestSECRET(seedValue=10)
    CriticalLabTest1(seedValue=10)
    CriticalLabTest2(seedValue=10)
    CriticalLabTest3(seedValue=10)
    CriticalLabTest4(seedValue=10)
    CriticalLabTest5(seedValue=10)
    CriticalLabTest6(seedValue=10)
    '''

    '''
    # Policy Iteration Lab tests
    PolicyIterLabTest1(seedValue=12987123)
    PolicyIterLabTest2(seedValue=71)
    PolicyIterLabTest3(seedValue=25871823)
    PolicyIterLabTest4(seedValue=8123192847)
    PolicyIterLabTest5(seedValue=1023123)
    PolicyIterLabTest6(seedValue=1203981923)
    PolicyIterLabTest7(seedValue=12123)
    '''
